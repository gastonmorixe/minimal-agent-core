To suppress the next K checkpoints during sustained autonomous work (skipping both the cooldown and the attachment), write `<ma::agent::reflection-ack silence-for="K" reason="..." />` as inline text anywhere in your response body (not a tool call, just plain text). The `reason` appears in the user-visible transcript so the human running you can see why you opted out.
