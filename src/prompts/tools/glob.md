Fast file-name and path pattern matching that works on any codebase size.

ALWAYS use this tool to find or count files by name or path. NEVER use `find`, `fd`, or `ls` through Bash for this. Supports glob patterns like `**/*.ts` or `src/**/*.{js,ts}`. Returns matching file paths sorted by modification time.
