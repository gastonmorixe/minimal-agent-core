{
  "name": "@minimal-agent/docs-tooling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isolated documentation toolchain: typedoc pinned to the TypeScript 6.0 compiler API. Quarantined here so the repo root can run TypeScript 7 while typedoc keeps the 6.0 programmatic API it still requires. See README.md for why.",
  "devDependencies": {
    "bun-types": "catalog:",
    "typedoc": "^0.28.20",
    "typescript": "^6.0.3"
  }
}
