{
  "id": "agent-identity",
  "name": "Agent Identity",
  "version": "0.1.0",
  "description": "Gives the agent an opt-in per-session display name, injected as one line in the system prompt. Off unless `agentName` (config) or MINIMAL_AGENT_AGENT_NAME (env) is set.",
  "promptFragments": [
    {
      "id": "name",
      "handler": {
        "type": "module",
        "path": "./handlers/identity.ts",
        "export": "default"
      },
      "timeoutMs": 1000,
      "order": 10
    }
  ]
}
