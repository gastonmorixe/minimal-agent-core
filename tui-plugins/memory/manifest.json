{
  "id": "memory",
  "name": "Memory",
  "version": "0.2.0",
  "description": "Persistent cross-session memory. The model emits <tui::memory> spans to save lessons; saved memories live in per-user files (`~/.minimal-agent/memory.md` for global, `~/.minimal-agent/projects/<cwd>/memory.md` for project-scoped) and are reloaded into the system prompt on every future session.",
  "prompt": "./PROMPT.md",
  "tuis": [
    {
      "id": "memory_save",
      "trigger": {
        "type": "inline_tag",
        "tag": "memory"
      },
      "handler": {
        "type": "module",
        "path": "./handlers/memory.ts",
        "export": "default"
      },
      "interactive": false
    }
  ],
  "promptFragments": [
    {
      "id": "memory_load",
      "handler": {
        "type": "module",
        "path": "./handlers/load.ts",
        "export": "default"
      },
      "timeoutMs": 1000,
      "order": 200
    }
  ]
}
